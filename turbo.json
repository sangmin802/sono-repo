{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "lostark:dev": {},
    "lostark:build": {
      "outputs": [".next/**", "!.next/cache/**"]
    }
  }
}
