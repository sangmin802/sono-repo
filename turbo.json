{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "setting": {
      "dependsOn": ["^setting"]
    },
    "lostark:dev": {},
    "lostark:build": {
      "outputs": [".next/**", "!.next/cache/**"]
    }
  }
}
