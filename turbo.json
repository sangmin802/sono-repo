{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "setting": {
      "dependsOn": ["^setting"]
    },
    "storybook": {},
    "lostark:dev": {},
    "lostark:build": {
      "outputs": [".next/**", "!.next/cache/**"]
    }
  }
}
